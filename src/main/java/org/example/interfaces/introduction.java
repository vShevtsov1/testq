package org.example.interfaces;

import org.example.entities.candidate;
import org.example.entities.manager;

public interface introduction {
    void introduction(manager manager, candidate candidate);
}
